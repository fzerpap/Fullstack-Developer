- if user_signed_in?
  .wrapper.wrapper-content.animated.fadeInRight
    .row
      /= render partial: '/production_orders/board'
    /.row
    /  .col-lg-12.text-center
    /    = image_tag('logo-apextool.png', width: 400, height: 200,   style: 'margin-top:150px')
    /    %h1 
    /      =I18n.t(:sistema)
    /    %h2
    /      = current_user.empresa_nome rescue ""
- else
  = render partial: 'log_in'

